extends layout.pug 

block content
  section#content 
    h2= text 
    if hint 
      p 
        i Hint: #{hint}
    //- else 
    //-   p (There is no hint.)
    a(href=`${id}?side=${sideToShow}`)= sideToShowDisplay
    br
    a(href='/cards') Next card